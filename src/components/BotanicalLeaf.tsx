import React from 'react';

// Les éléments botaniques en SVG ont été retirés conformément à la demande du client
export const BotanicalLeaf: React.FC<{ className?: string; flip?: boolean; opacity?: number }> = () => {
  return null;
};
